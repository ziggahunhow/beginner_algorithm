// Question 20 on Leetcode

const isValid = function(s) {
  const A = s.split('');
  let obj = {}
  for (let i = 0; i < s.length; i++) {
    if()
  }
};

isValid('()');
// isValid('([)]');

// logic:
// for loop of the string
// if theres ( or { or [
// expect corresponding parts
// if not, return false
// otherwise save corresponding parts to obj with their their indexes
// Object.values(obj) if not in ascending order? false : true
